tsc = ../../node_modules/.bin/tsc
jest = ../../node_modules/.bin/jest

compile: clean
	$(tsc)
	@echo "compile zone-context successfully 👏\n"

clean:
	rm -rf ./lib
	@echo "clean zone-context successfully 👏\n"