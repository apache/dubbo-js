{
  "name": "dubbo-registry",
  "root": "packages/dubbo",
  "sourceRoot": "packages/dubbo-registry/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc",
        "cwd": "packages/dubbo-registry"
      }
    },
    "ts-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc --noEmit",
        "cwd": "packages/dubbo-registry"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx jest",
        "cwd": "packages/dubbo-registry"
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"**/*.{ts,tsx,md}\"",
        "cwd": "packages/dubbo-registry"
      }
    }
  }
}
