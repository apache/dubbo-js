{
  "name": "dubbo-common",
  "root": "packages/dubbo-common",
  "sourceRoot": "packages/dubbo-common/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc",
        "cwd": "packages/dubbo-common"
      }
    },
    "ts-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc --noEmit",
        "cwd": "packages/dubbo-common"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx jest",
        "cwd": "packages/dubbo-common"
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"**/*.{ts,tsx,md}\"",
        "cwd": "packages/dubbo-common"
      }
    }
  }
}
